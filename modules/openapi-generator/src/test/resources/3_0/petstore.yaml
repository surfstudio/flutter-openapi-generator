openapi: "3.0.0"
info:
  version: 1.0.0
  title: Swagger Sandbox

components:
  schemas:
    EnumTest:
      type: string
      enum:
        - FORMAT_CAT
        - FORMAT_DOG

paths:
  /createAnimal:
    post:
      responses:
        '200':
          description: 'response'
          content:
            application/json:
              schema:
                $ref: 'EnumTest'